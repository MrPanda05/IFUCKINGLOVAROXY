import React from 'react'

type Header= {
    text: string
}

const Head = ({
 text
}: Header) => {
    return(
        <header>

        </header>
    )
}

const Header = () => {
  return (
    <header>
        Header
    
    </header>
  )
}

export default Header